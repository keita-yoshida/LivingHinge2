# リビングヒンジ ジェネレーター — 開発引き継ぎ資料

レーザー加工用カーフベンディング/リビングヒンジパターンを生成するWebアプリ。
単一HTMLファイル(`livinghinge.html`)として完結し、WordPressのuploads配下に置くだけで公開できる。

- 公開URL: https://laser-kakouki.com/wp-content/uploads/2025/12/livinghinge.html
- 外部依存: **完全にゼロ**(v6でGoogle Fonts読み込みを削除。フォントはOS標準のみ)
- 現行サイズ: 約75KB / 約1,660行
- 作業ファイル名は `livinghinge_2.html`(公開時に `livinghinge.html` へリネーム)

---

## 1. 絶対に壊してはいけない契約(Hard Contract)

1. **DXF出力**: R12形式(AC1009)、単位mm実寸、レイヤー名 `CUT`。
   2点線分は `LINE`、多点は `POLYLINE`+`VERTEX`+`SEQEND`。Y軸は反転して出力(`y' = H - y`、DXFはY上向きのため)。
   LightBurn / Illustrator / Fusion 360 / Inkscape で読めることを維持する。
2. **SVG出力**: `width="{W}mm" height="{H}mm" viewBox="0 0 W H"`(=1ユーザー単位が1mm)、
   `stroke="#FF0000" stroke-width="0.1"` fill無し。テキスト要素は含めない。
3. **単一ファイル・依存ゼロ**を維持する。3Dも自前Canvasレンダラーであり、Three.js等を導入しない
   (CDN障害・オフラインで壊れるため。過去に明示的に不採用を決定済み)。
   **外部フォントも読み込まない**(v6でGoogle Fontsを削除済み。再導入しないこと)。
4. **設定共有URL**: `location.hash` にJSONを `encodeURIComponent` して保持。
   後方互換を守ること(古いキー、例: 廃止済み `p.web` が入っていても無視して動くこと。テスト済み)。
   新キーを足す場合は、そのキーが**無い**古いhashでもデフォルトにフォールバックすること
   (v6で `mat` を追加。旧hash読み込み時は `mat` 未定義→デフォルト適用をテスト済み)。
5. ブラウザストレージ(localStorage等)は使わない。状態共有はhashのみ。

## 2. ファイル構成(単一ファイル内のセクション)

```
livinghinge.html
├─ <style>            デザイントークン(:root CSS変数)、パネル/ステージ/3Dオーバーレイ
├─ <body>             ヘッダー / 左パネル(パターン・プリセット・サイズ・調整・DL・ヒント)
│                     / ステージ(表示モード切替・ズーム・viewport・統計チップ)
│                     viewport内: <canvas #pv3d>(3D) + .bendbar(3D操作) + <svg #pv>(2D)
└─ <script>
   ├─ state / GLOBAL_DEFS / PARAM_DEFS     状態とスライダー定義
   ├─ 幾何ユーティリティ                    clipHalf / clipRect / polyLen / columnXs / centerYs
   ├─ GEN = {straight,wave,diamond,cross,arc,hex,hexslit,bone,tri,spiral}  ← 全10パターン生成器(mm座標のポリライン配列を返す)
   ├─ PATTERNS / PRESETS
   ├─ MATERIALS / flexScoreOf / estMinRadius / genPolysFor   材料プロファイルと半径推定(§10)
   ├─ regenerate()                          生成→renderView()→統計→警告→hash更新
   ├─ 2Dプレビュー                          renderPreview / fitView / zoomAt(SVG viewBox操作)
   ├─ 3Dレンダラー                          build3d / render3d / fit3d / zoom3d(§4参照)
   ├─ 入力系                                pointer(パン/回転/ピンチ共用、bendbar除外)/ wheel / ボタン
   ├─ コントロールUI生成                     ctlHTML / bindCtl / buildGlobalCtls / buildParamCtls / buildPatternCards
   ├─ エクスポート                          makeDXF(polys?,H?) / makeSVG(polys?,W?,H?) / download
   │                                        引数省略時は現在の盤面。テストピースは引数渡しで再利用(§11)
   ├─ テストピース                          TEST / testPieceVariants / buildTestSheet / downloadTestSheet
   └─ hash共有 / toast / init
```

ポリラインは `[[x,y],...]` のmm座標。原点は左上、y下向き。`cuts`(生成結果)と `framePoly`(外枠)が描画・出力の唯一のソース。

## 3. パターン生成仕様

### 3.1 共通(列ベース: straight / wave / diamond / cross / arc)
- `columnXs(x0,x1,pitch)`: 領域内に対称配置した列中心X群。
- `centerYs(y0,y1,period,offset)`: period = `cut + gap`。偶数列はoffset 0、奇数列はperiod/2ずらし(千鳥)。
- 生成後は必ず `clipRect` で余白内にクリップ(開ポリライン対応の半平面クリップを4回)。
- diamond/crossは形状幅を `pitch - 0.6〜0.8` で自動制限。

### 3.2 hex(ハニカム)
尖頭六角形(pointy-top)。各六角形を左右2本のポリラインで描き、上下頂点の隣接辺を
`gap/2` ずつトリムしてブリッジを残す。colStep=`√3R+hs`、rowStep=`1.5R+hs*0.87`、奇数行は半列オフセット。

### 3.3 spiral(メアンダー)★最重要・今回の主変更点
**[4,2] 2Dメアンダーパターン**(Dujam Ivanišević / koFAKTORlab 2014年考案)の忠実な実装。
形式化の出典: Zarrinmehr et al. "Kerfing with Generalized 2D Meander-Patterns" (CAADFutures 2017)。

構造の要点(実装を触る前に必ず理解すること):
- 正方形クワッド格子。各クワッドに**同キラリティの矩形スパイラル2本**を対角アンカーで絡ませる。
- カットは格子頂点で合流し「4本枝のスパイラルツリー」になる。頂点は市松2彩色され、
  片方の色(ツリー頂点)からだけ渦が生え、もう片方(エッジ中点相当)は**無傷のソリッド固定点**として残る。
- ウェブ(セル間の枠)は**存在しない**。材料は固定点同士を結ぶ蛇行アームのみ。これが二重曲率の柔軟性の源。

実装詳細(`GEN.spiral`):
- アーム幅 `sw` はセルサイズ `cs` の**奇数分割に自動スナップ**: `N = round(cs/sw)` を奇数化(最小5)、`p = cs/N`。
  → 渦間隔が全域で完全に均一になる数学的条件。
- 基本パスA(アンカー(0,0)、自ピッチ2p)の頂点列:
  `(0,0) → (S-p,0) → (S-p,S-p) → (2p,S-p) → (2p,2p) → (S-3p,2p) → (S-3p,S-3p) → (4p,…) …`
  (k%4でR/U/L/D、インセットは1周ごとに2p増。線長がp以下になったら終了)
- B = Aの180°回転(同一クワッド内で絡む相手)。
- 奇数パリティのクワッドは `rot90: (x,y)→(S-y,x)` を適用(**ミラーではなく回転**。キラリティを全クワッドで統一するため。ここをミラーに変えると本物と異なる構造になる)。
- 偶数クワッドは水平辺上に、奇数クワッドは垂直辺上にカットを持つため、共有辺のカットは重複しない(設計済み)。

### 3.3.5 bone / hexslit / tri ★v7で追加(fequalsf再現)
fequalsf.com の "parametric kerf" テストフォブDXF(#6/#7, #8, #9)を解析して再現したもの。
DXFはR13(AC1012)・SPLINE主体。**スプラインは短い断片の連鎖なので、端点連結(join)して
初めて本当のモチーフ形状が分かる**(断片単位のbbox統計では誤同定する。v7初版はこれで
縦の鼓形と誤実装し、やり直した)。手順は§6.7。

- **bone**(#6/#7): 参照モチーフは **幅1.205×高さ0.133の滑らかな閉曲線** =
  中央レンズ(半幅A≈0.055×全長) → 細い首(≈0.13A) → 両端の丸バルブ(r≈0.7A)。
  半幅プロファイルを円弧+コサイン補間で構成し、`straight` と同じ列/千鳥配置。
  レンズ内部は脱落する(仕様)。パラメータ pitch/cut/gap/**br**(=レンズ半幅)。
- **hexslit**(#8): **フラットトップ・縦0.8スカッシュの六角形**。各セル =
  **完全に閉じた内六角形(芯は脱落する。参照フォブも同じ)** + コーナー3箇所に
  ブリッジを残した外郭線(2辺ずつ3ピース) + セル間の帯 `hs`。
  パラメータ hr/hs/**rw**(リング幅=外郭−内六角)/gap(コーナーブリッジ)。
  参照比率: 内/外 ≈ 0.73 → rw標準1.2(hr=5時)。
- **tri**(#9): 三角格子(辺 **ts**)。各セルに同心の内接三角形リング(間隔 **td**)。
  **偶数リングは角で、奇数リングは辺中央で切れ目**(交互ブリッジ)。セルの辺自体は
  切らないので格子沿いに帯が通り、**内部は何も脱落しない**(§6.2適用可)。
  ただし margin=0 だと外周クリップで小片が落ちるため余白2mm推奨(警告実装済み。
  margin=2で全プリセット連結成分1を確認済み)。パラメータ ts/td/gap。

### 3.4 パターン追加の手順
1. `GEN` に生成器を追加(mm座標ポリライン配列を返す。最後に `clipRect`)。
2. `PARAM_DEFS` にパラメータ定義、`state.p` にデフォルト、`PATTERNS` に登録。
3. `PRESETS` 3種にキーを追加。
4. `renderStats` の `charLen`(柔軟性ヒューリスティック)と `renderWarnings` に分岐を追加。
5. サムネイルは同じ生成器で自動描画される(`thumbSVG` のパラメータセットに追記)。
6. §6の検証バッテリーを必ず通す。

## 4. 3D曲げプレビュー(自前レンダラー)

Canvas 2D + ペインターズアルゴリズム。プリミティブは quad(板メッシュ)と line(カット線)。

- **曲げ形状** `s3.shape`:
  - `cyl` 円筒: 弧長保存の正確な円筒マッピング。R = 弧長/θ。メッシュはu方向ストリップのみ(nW=1)。
  - `dome` ドーム: 球面キャップ。中心からの距離ρを半径方向に弧長保存でマッピング
    (`r = R·sin(ρ/R)`, `z = -R(1-cos(ρ/R))`、ρ/Rはπ×0.98でクランプ)。
  - `saddle` サドル: 双曲放物面 `z = κ(u²-w²)/2×0.9`, κ=1/R。
- 法線は**数値微分**(surf を±0.5mmで差分→外積)で汎用に計算。板厚オフセットもこれで裏面を生成。
  新しい曲面を足すときは `surf(u,w)` を1個書くだけでよい。
- カット線は曲率に追従するよう **u/w両方向にステップ細分**(`du=stepU*0.9` 等)して面の0.25mm外側に浮かせる(Zファイト回避)。
- Z再センタリングは生成後にbboxで一括シフト。
- 性能予算: build3d+render3d で **25ms以下**(現状: cyl <5ms、dome/saddle 約20ms)。
  スライダー入力は `scheduleBuild()`(rAF集約)経由にすること。
- カメラ: yaw/pitch軌道 + dist。`fit3d()` が基準距離。ズーム表示は `fitDist/dist`。

## 5. 入力系の重要な落とし穴(実際に踏んだバグ)

`viewport` の pointerdown で `setPointerCapture` を呼ぶため、**viewport内に置いたUI(bendbar)への
実クリック/実ドラッグが全て奪われて死ぬ**。現在は
`if(e.target.closest(".bendbar")) return;` で除外している。
viewport内にインタラクティブ要素を追加する場合は必ずこの除外リストに加えること。

教訓: `dispatchEvent(new Event('input'))` のような**合成イベントのテストではこのバグは検出できない**。
UIテストは Playwright の `mouse.down/move/up` による実ポインタ操作で行うこと。

## 6. 検証バッテリー(リリース前に必ず実行)

このプロジェクトでは「上位工程=判断・受け入れ基準の設定、下位工程=実装・自動検証」の分離で開発してきた。
以下が確立済みの受け入れ基準。リポジトリ化したら `tests/` に移植すること(vitest + Playwright推奨)。

### 6.1 幾何(Node、DOMスタブでGENを直接呼ぶ)
- 全パターン×両方向で: NaNなし、境界外座標なし、ポリライン数・総カット長が妥当。
- meander固有: スナップ後pの均一性(平行走査線の材料ラン幅分布)。

### 6.2 材料連結性(meander系は必須・最重要)
カットをカーフ0.3mm相当に太らせて0.1mm/pxでラスタライズ → 材料をフラッドフィル →
**連結成分数が1であること**(=切り落ちる破片ゼロ)。
全プリセット+極端値(cs=5/sw=0.8、cs=40/sw=5、非整数比)で実施。実装は過去テスト
(`test4.js` 相当)を参照。

### 6.3 目視代替: ASCIIラスタ可視化
ラスタを `#`/`.` で20mm四方ほど出力すると、画像ビューアなしでトポロジーを確認できる。
メアンダーの絡み合い・固定点の存在確認に非常に有効だった。デバッグ手段として残すこと。

### 6.3.5 検証環境の落とし穴(v6で踏んだ)

- **`file://` で開くとブラウザが強烈にキャッシュし、編集が反映されない**。
  リロードもクエリ付与も効かなかった。`.claude/launch.json` の `static` 設定で
  `python -m http.server 8777` を起動し、`http://localhost:8777/livinghinge_2.html` で検証すること。
- 検証ツールによっては **canvasのピクセル読み出し(`getImageData`)とスクリーンショットがハングする**。
  §6.4の「Canvasピクセル解析」が使えない場合は、`prims.length` と build+render の実測msで代替する。

### 6.4 ブラウザE2E(Playwright)
- 7パターン切替、プリセット、実ドラッグでのスライダー/回転共存(§5)、3D形状3種の描画差分
  (Canvasピクセル解析: 木色px数・カット線px数・シルエット幅で判定)。
- DXF/SVGダウンロードの実ファイル検証(POLYLINE数、`EOF`終端、SVGヘッダ)。
- hash往復復元、旧形式hash(`p.web` 入り)での無エラー起動。

## 6.5 材料プロファイルと目標半径モード(v6)

`state.mat = {kind, thick}`。素材係数 `MATERIALS[kind].k` は MDF 1.0 / シナベニヤ 0.85 / アクリル 1.7。

- **半径推定**: `R_min ≈ 9 × k × 板厚 / effScore(柔軟性スコア)`。
  `effScore` はスコア1.5を膝として傾き0.4に圧縮する。**この圧縮は必須**で、
  無いとメアンダーのような高カット密度パターンで推定半径が非現実的に小さくなる。
- **逆算ソルバー** (`#btnSolve`): 「かたい端」と「柔らかい端」のパラメータ集合の間を
  線形補間し、**実際に生成して実スコアを測りながら18回二分探索**する。
  閉形式の逆関数を使わないのは、ゲージ表示と逆算結果を必ず一致させるため。
  端でクランプした場合はトーストでその旨を伝える(無言で外れた値を返さない)。
- 補間レンジ: 列系 = pitch/cut/gap、hex = hr/hs/gap、spiral = **sw のみ**
  (csも動かすと渦の見た目が別物になるため、セルサイズはユーザーの指定を尊重する)。
- 板厚は3Dプレビューと**単一の `setThickness()` で同期**。3D側スライダーもこれを呼ぶ。

精度は主要域(R=25〜150mm)で誤差10%程度。パターンによっては到達できない領域があり、
特にメアンダーは構造上つねに柔らかく、R≈10〜15mm付近から上に動かせない(仕様)。

## 6.6 加工時間の目安(v6)

総カット長 ÷ 加工速度(5/10/20/40 mm/s から選択)。加減速・移動時間は含まない旨をtitle属性に明記。
警告は従来の「カット数 4,000本超」に加え、**総カット長 20m超**でも出るようにした
(メアンダーは本数が少なくても長大になるため。§7の既知課題を解消)。

## 6.7 外部DXFの解析手順(v7で確立)

参考パターンのDXFから形状を読み取って再現するときの手順。実際に fequalsf の
"parametric kerf" シート(R13/AC1012, SPLINE 3357本+POLYLINE 1544本)で使った:

1. group code をペアで読み、code 0 の値でエンティティを分類(SPLINE/POLYLINE/VERTEX/MTEXT等)。
2. **SPLINEは De Boor で評価**(次数71/制御点10,20/knot 40)。制御点を直線で結ぶと曲線が潰れる。
2.5. **評価済み曲線を端点で連結(join)してから形状を判断する**。CADのスプラインは1本の
   モチーフが多数の短い断片に分かれており、断片単位のbbox統計で判断すると誤同定する
   (v7でこれを踏み、boneを一度誤実装した)。連結後に閉/開・寸法・周長を測る。
3. **PNGに手書きラスタライズして Read で目視**(§6.3.5の通りブラウザのスクショ/getImageDataはハングする)。
   純Python(zlib+struct)でPNGを書けば画像ビューア不要。単位はインチのことが多いが、
   本ツールは自前mm寸法で再生成するので単位は無視してトポロジーと比率だけ取る。
4. MTEXT(code 1/3)にタイトルや出典が入っていることがある(今回 "fequalsf.com" 判明)。

## 7. 既知の制約・仕様上の注意

- メアンダーはカット総長が他パターンの約2倍以上(120×70mm標準で約6,140mm)。v6で総カット長ベースの警告と時間見積りを追加済み(§6.6)。
- **閉ループを切るパターンは内側が脱落する**。これはパターン本来の意図であり不具合ではない:
  diamond 約12%(連結成分81) / bone 約24%(レンズ内部) / **hexslit 約46%(六角形の芯。参照フォブ#8も同じ設計)**。
  §6.2の「連結成分1」基準を適用してよいのは **メアンダーと tri(margin≥2)** のみ。
  hex/tri は margin=0 だと外縁クリップで小片が出る(triは余白2mm推奨の警告を実装済み)。
- (v7初版の「hexslitは単線アウトライン・連結成分1」という設計は参照DXFと構造が違ったため撤回。
  現行は参照に忠実な「閉じた内六角+外郭ブリッジ」方式。芯の脱落は仕様であり警告で明示している。)
- メアンダーのツリー頂点では4本のカットが1点で合流し、近傍の材料が先細りになる。これは本物のパターン固有の性質であり不具合ではない(ただし極小sw時は脆い)。
- 3Dのドーム/サドルは「形状イメージ」であり物理シミュレーションではない(UI上も明記済み)。ドームマッピングは半径方向のみ等長で面積は歪む。
- 柔軟性ゲージはヒューリスティック(カット密度×√特性長)。目安表示に留めること。
- ブラウザの `hex` パターンで外部クリップにより稀に短い断片が出るのは `polyLen>0.3` で除去済み。

## 7.5 テストピース面付け(v6)

70×30mm の小片を、主要パラメータを現在値の ×0.6〜×1.4 で5段階に振って縦に面付けする
(`buildTestSheet()`)。振るパラメータは 列系(bone含む)=`gap` / hex・hexslit=`hs` / tri=`td` / spiral=`sw`。

- 左右に `grip` 8mm の掴みしろを残し、その間にパターンを生成する。
- **識別はテキストではなく切り欠きの本数**(下辺から3mmの切り込み × 1〜5本)。
  SVG契約(§1-2)がテキスト要素を禁じているため。ここをtextに変えないこと。
- 出力は `makeDXF/makeSVG` に polys と寸法を渡して再利用する(本体出力の契約と同一)。

## 8. ロードマップ候補(優先度順の提案)

v6で 1(フォント)/ 2(材料プロファイル)/ 3(目標半径)/ 6(テストピース)を実装済み。残り:

1. **メアンダーの一般化**(Zarrinmehr方式): 六角形・8.8.4タイリング版、画像による局所剛性制御(論文ではFEAで8.8.4が最柔軟と報告)
2. カーフ幅補正オプション(DXFオフセット出力)
   — 開ポリラインの正確なオフセットは自己交差処理が必要で、実用上はgap調整でほぼ吸収できるため優先度は低いと判断
3. プロジェクト保存/読込(JSON)— senjafuda designerと共通様式にすると良い
4. 半径推定の実測キャリブレーション: §6.5の係数(9 / 素材k / 膝1.5)は理論値ではなく手当て。
   テストピース機能で実測データが取れるようになったので、実曲げ結果と突き合わせて係数を更新するとよい
5. Vite化する場合: `geometry/`(GEN+クリップ)、`render2d/`、`render3d/`、`ui/`、`export/` に分割し、
   `vite-plugin-singlefile` で単一HTML出力を維持する(契約§1-3)

## 9. 開発履歴サマリー

- v1(公開中の旧版): 直線・ひし形のみ、DXF出力。
- v2: 6パターン化、木板/レーザープレビュー、ズーム/パン、統計、プリセット、SVG出力、hash共有。
- v3: 3D曲げプレビュー追加(自前レンダラー、円筒)。pointer capture問題の修正。
- v4: 曲げ形状にドーム/サドル追加(数値法線の汎用サーフェス化)。
- v5: スパイラル暫定実装を破棄し、正式な[4,2]メアンダーパターンに置換。
  奇数分割スナップ、連結性検証バッテリー確立。パターン名「メアンダー」。
- v6: 「パラメータを与える」道具から「作りたいものを与える」道具へ。
  材料プロファイル(素材×板厚)と目標半径の逆算ソルバー(§6.5)、テストピース面付け(§7.5)、
  加工時間の目安と総カット長警告(§6.6)、Google Fonts依存の完全排除。
  板厚を材料パネルと3Dプレビューで一元化。エクスポート関数を任意ポリライン対応に一般化。
- v7(現行): パターンを7→10種に拡張。fequalsf.com の "parametric kerf" DXFを解析(§6.7)し、
  bone(ドッグボーン #6/#7)・hexslit(スリット六角 #8)・tri(入れ子三角 #9)を追加(§3.3.5)。
  初版はスプライン断片単位の解析でモチーフを誤同定して作り直した(断片は端点連結してから見ること)。
  DXF解析ワークフロー(SPLINE De Boor評価→端点連結→純Python PNG→目視)を確立。

出典クレジット(アプリ内ヒント欄にも記載済み):
パターン考案 = Dujam Ivanišević (koFAKTORlab, 2014) / 形式化 = Zarrinmehr, Akleman, Ettehad, Kalantar, Borhani (CAADFutures 2017, "[4,2] meander pattern")
